<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- course_type -->
<mapper namespace="cn.ggx.dao.CourseTypeDao">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="courseTypeMap" type="courseType">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="flag" property="flag" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        ct.id, ct.name, ct.flag, ct.create_time
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->

    <!-- 查询所有 -->
    <select id="findCourseTypeAll" resultMap="courseTypeMap">
        SELECT
            <include refid="allColumns" />
        FROM course_type ct
    </select>

    <!-- 根据条件参数查询列表 -->
    <select id="findCourseTypeByCondition" resultMap="courseTypeMap" parameterType="map">
        SELECT
            <include refid="allColumns" />
        FROM course_type ct WHERE 1 = 1
        <if test="name != null and name != ''">
            AND ct.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="flag != null">
            AND ct.flag = #{flag}
        </if>
        <if test="createTime != null">
            AND ct.create_time = #{createTime}
        </if>
    </select>

    <!-- 根据主键查询信息 -->
    <select id="findCourseTypeByIds" resultMap="courseTypeMap" parameterType="list">
        SELECT
            <include refid="allColumns" />
        FROM course_type ct WHERE ct.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增信息 -->
    <insert id="addCourseType">
        INSERT INTO course_type (
            id, name, flag, create_time
        ) VALUES (
            #{id},
            #{name},
            #{flag},
            #{createTime}
        )
    </insert>

    <!-- 批量新增信息 -->
    <insert id="addCourseTypes" parameterType="list">
        INSERT INTO course_type (
            id, name, flag, create_time
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.name},
                #{item.flag},
                #{item.createTime}
            )
        </foreach>
    </insert>

    <!-- 修改信息 -->
    <update id="updateCourseType">
        UPDATE course_type
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="flag != null">
                flag = #{flag},
            </if>
            <if test="createTime != null">
                create_time = #{createTime}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 批量修改信息 -->
    <update id="updateCourseTypes" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE course_type
            <set>
                <if test="item.name != null">
                    name = #{item.name},
                </if>
                <if test="item.flag != null">
                    flag = #{item.flag},
                </if>
                <if test="item.createTime != null">
                    create_time = #{item.createTime}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteCourseTypeById" parameterType="int">
        DELETE FROM course_type WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除 -->
    <delete id="deleteCourseTypeByIds" parameterType="list">
        DELETE FROM course_type WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>