<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- course_video -->
<mapper namespace="cn.ggx.dao.CourseVideoDao">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="courseVideoMap" type="courseVideo">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="video_name" property="videoName" jdbcType="VARCHAR" />
        <result column="flag" property="flag" jdbcType="VARCHAR" />
        <result column="free_flag" property="freeFlag" jdbcType="VARCHAR" />
        <result column="topic_id" property="topicId" jdbcType="INTEGER" />
        <result column="play_url" property="playUrl" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        cv.id, cv.video_name, cv.flag, cv.free_flag, cv.topic_id, cv.play_url, cv.create_time
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->

    <!-- 查询所有 -->
    <select id="findCourseVideoAll" resultMap="courseVideoMap">
        SELECT
            <include refid="allColumns" />
        FROM course_video cv
    </select>

    <!-- 根据条件参数查询列表 -->
    <select id="findCourseVideoByCondition" resultMap="courseVideoMap" parameterType="map">
        SELECT
            <include refid="allColumns" />
        FROM course_video cv WHERE 1 = 1
        <if test="videoName != null and videoName != ''">
            AND cv.video_name LIKE CONCAT('%', #{videoName}, '%')
        </if>
        <if test="flag != null">
            AND cv.flag = #{flag}
        </if>
        <if test="freeFlag != null">
            AND cv.free_flag = #{freeFlag}
        </if>
        <if test="topicId != null">
            AND cv.topic_id = #{topicId}
        </if>
        <if test="playUrl != null and playUrl != ''">
            AND cv.play_url LIKE CONCAT('%', #{playUrl}, '%')
        </if>
        <if test="createTime != null">
            AND cv.create_time = #{createTime}
        </if>
    </select>

    <!-- 根据主键查询信息 -->
    <select id="findCourseVideoByIds" resultMap="courseVideoMap" parameterType="list">
        SELECT
            <include refid="allColumns" />
        FROM course_video cv WHERE cv.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增信息 -->
    <insert id="addCourseVideo">
        INSERT INTO course_video (
            id, video_name, flag, free_flag, topic_id, play_url, create_time
        ) VALUES (
            #{id},
            #{videoName},
            #{flag},
            #{freeFlag},
            #{topicId},
            #{playUrl},
            #{createTime}
        )
    </insert>

    <!-- 批量新增信息 -->
    <insert id="addCourseVideos" parameterType="list">
        INSERT INTO course_video (
            id, video_name, flag, free_flag, topic_id, play_url, create_time
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.videoName},
                #{item.flag},
                #{item.freeFlag},
                #{item.topicId},
                #{item.playUrl},
                #{item.createTime}
            )
        </foreach>
    </insert>

    <!-- 修改信息 -->
    <update id="updateCourseVideo">
        UPDATE course_video
        <set>
            <if test="videoName != null">
                video_name = #{videoName},
            </if>
            <if test="flag != null">
                flag = #{flag},
            </if>
            <if test="freeFlag != null">
                free_flag = #{freeFlag},
            </if>
            <if test="topicId != null">
                topic_id = #{topicId},
            </if>
            <if test="playUrl != null">
                play_url = #{playUrl},
            </if>
            <if test="createTime != null">
                create_time = #{createTime}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 批量修改信息 -->
    <update id="updateCourseVideos" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE course_video
            <set>
                <if test="item.videoName != null">
                    video_name = #{item.videoName},
                </if>
                <if test="item.flag != null">
                    flag = #{item.flag},
                </if>
                <if test="item.freeFlag != null">
                    free_flag = #{item.freeFlag},
                </if>
                <if test="item.topicId != null">
                    topic_id = #{item.topicId},
                </if>
                <if test="item.playUrl != null">
                    play_url = #{item.playUrl},
                </if>
                <if test="item.createTime != null">
                    create_time = #{item.createTime}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteCourseVideoById" parameterType="int">
        DELETE FROM course_video WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除 -->
    <delete id="deleteCourseVideoByIds" parameterType="list">
        DELETE FROM course_video WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>